# CDR File Extractor

Simple Python tool that rebuilds CorelDRAW (.cdr) files from extracted Corel package folders.

## Usage

Place your extracted Corel folder in the same directory and run:

python src/reconstruir_cdr.py

The script will rebuild the CDR container automatically.

## Requirements

Python 3.13+
No external libraries required.
